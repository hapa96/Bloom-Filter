package fhnw.dist.p2;

import java.util.Scanner;

public class BloomFilter {

	public static void main(String[] args) {
		// Gegebene Anzahl n von zu erwartenden Elementen
		int n = 5;
		// Fehlerwahrscheinlichkeit p
		Scanner in = new Scanner(System.in);
		double p = Double.valueOf(in.nextLine());
		System.out.println(p);
		// Filtergrösse m (wird berechnet)
		int m;
		// optimale Anzahl k an Hashfunktionen (wird berechnet)
		int k;
	}

}
